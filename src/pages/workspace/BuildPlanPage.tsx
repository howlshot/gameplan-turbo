import { PromptLabPage } from "@/pages/workspace/PromptLabPage";

// Legacy compatibility export. Old build-plan entry points now land in Prompt Lab guided planning.
export const BuildPlanPage = (): JSX.Element => <PromptLabPage />;
