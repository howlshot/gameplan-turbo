# Assumptions

- Brand: `Gameplan Turbo`
- v1 remains browser-only and local-first
- No new backend is added
- The AI provider layer remains BYOK
- The first built-in template is `Arcade Action / Rail Shooter`
- Engine bias stays engine-agnostic by default
- Prompt Lab should still produce useful output without provider keys via local fallbacks
- Legacy app-centric docs and code may remain in the repo during the fork transition, but the visible product path is game-first
